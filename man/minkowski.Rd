% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta_div.r
\name{minkowski}
\alias{minkowski}
\title{Minkowski distance}
\usage{
minkowski(
  counts,
  norm = "percent",
  power = 1.5,
  margin = 1L,
  pairs = NULL,
  cpus = n_cpus()
)
}
\arguments{
\item{counts}{A numeric matrix of count data (samples \eqn{\times} features).
Also supports \code{phyloseq}, \code{rbiom}, \code{SummarizedExperiment}, and
\code{TreeSummarizedExperiment} objects. See \code{vignette('performance')} for
optimizing large datasets.}

\item{norm}{Normalize the incoming counts. Options are:
\itemize{
\item \code{'percent'}: Relative abundance (sample abundances sum to 1).
\item \code{'binary'}: Unweighted presence/absence (each count is either 0 or 1).
\item \code{'clr'}: Centered log ratio.
\item \code{'none'}: No transformation.
}

Default: \code{'percent'}, which is the expected input for these formulas.}

\item{power}{Scaling factor for the magnitude of differences between
communities (\eqn{p}). Default: \code{1.5}}

\item{margin}{The margin containing samples. \code{1} if samples are rows,
\code{2} if samples are columns. Ignored when \code{counts} is a special object
class (e.g. \code{phyloseq}). Default: \code{1}}

\item{pairs}{Which combinations of samples should distances be
calculated for? The default value (\code{NULL}) calculates all-vs-all.
Provide a numeric or logical vector specifying positions in the
distance matrix to calculate. See examples.}

\item{cpus}{How many parallel processing threads should be used. The
default, \code{n_cpus()}, will use all logical CPU cores.}
}
\description{
A generalized metric that includes Euclidean and Manhattan distance as special cases.
}
\details{
The Minkowski distance is defined as:
\deqn{\sqrt[p]{\sum_{i=1}^{n} (P_i - Q_i)^p}}

Where:
\itemize{
\item \eqn{P_i}, \eqn{Q_i} : Proportional abundances of the \eqn{i}-th feature.
\item \eqn{n} : The number of features.
\item \eqn{p} : The geometry of the space (power parameter).
}

\strong{Parameter: power}

The \code{power} parameter (default 1.5) determines the value of \eqn{p} in the equation.

\strong{Special Cases}
\itemize{
\item \strong{Manhattan distance}: When \eqn{p = 1}, the formula reduces to the sum of absolute differences.
\item \strong{Euclidean distance}: When \eqn{p = 2}, the formula reduces to the standard straight-line distance.
\item \strong{Chebyshev distance}: When \eqn{p \to \infty}, the formula reduces to the maximum absolute difference.
}

Base R Equivalent:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{p <- 1.5
sum(abs(x - y)^p) ^ (1/p)
}\if{html}{\out{</div>}}
}
\examples{
    minkowski(ex_counts, power = 2) # Equivalent to Euclidean
}
\references{
Deza, M. M., & Deza, E. (2009). Encyclopedia of distances. Springer.

Minkowski, H. (1896). \emph{Geometrie der Zahlen}. Teubner.
}
\seealso{
\code{beta_div()}, \code{vignette('bdiv')}, \code{vignette('bdiv_guide')}

Other Abundance metrics: 
\code{\link{aitchison}()},
\code{\link{bhattacharyya}()},
\code{\link{bray}()},
\code{\link{canberra}()},
\code{\link{chebyshev}()},
\code{\link{chord}()},
\code{\link{clark}()},
\code{\link{divergence}()},
\code{\link{euclidean}()},
\code{\link{gower}()},
\code{\link{hellinger}()},
\code{\link{horn}()},
\code{\link{jensen}()},
\code{\link{jsd}()},
\code{\link{lorentzian}()},
\code{\link{manhattan}()},
\code{\link{matusita}()},
\code{\link{morisita}()},
\code{\link{motyka}()},
\code{\link{psym_chisq}()},
\code{\link{soergel}()},
\code{\link{squared_chisq}()},
\code{\link{squared_chord}()},
\code{\link{squared_euclidean}()},
\code{\link{topsoe}()},
\code{\link{wave_hedges}()}
}
\concept{Abundance metrics}
