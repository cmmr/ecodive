# documentation

documentation

## Arguments

- counts:

  A numeric matrix of count data (samples \\\times\\ features) or a
  numeric vector representing a single sample. Counts are automatically
  converted to presence/absence (binary) values prior to calculation.
