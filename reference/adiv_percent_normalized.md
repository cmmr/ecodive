# documentation

documentation

## Arguments

- counts:

  A numeric matrix of count data (samples \\\times\\ features) or a
  numeric vector representing a single sample. Raw counts are
  automatically converted to relative abundances (proportions).
