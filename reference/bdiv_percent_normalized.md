# documentation

documentation

## Arguments

- counts:

  A numeric matrix of count data (samples \\\times\\ features). Counts
  are automatically converted to relative abundances (proportions
  summing to 1) prior to calculation.
