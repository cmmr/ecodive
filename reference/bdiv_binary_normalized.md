# documentation

documentation

## Arguments

- counts:

  A numeric matrix of count data (samples \\\times\\ features). Counts
  are automatically converted to presence/absence (binary) values prior
  to calculation.
