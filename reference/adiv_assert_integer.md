# documentation

documentation

## Arguments

- counts:

  A numeric matrix of count data (samples \\\times\\ features) or a
  numeric vector representing a single sample. Values must be integers
  (non-integer counts will return an error).
